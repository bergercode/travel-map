"use strict";
// This file is used to communicate limitations to the user.
// The user asked if the train can uses the train network.
// Since a public free rail routing API is not available (OSRM doesn't support it natively without a custom server),
// and implementing a full rail router is out of scope/capacity for this FE demo,
// we will maintain the "Direct Line" visual which is standard for showing connections where precise routing data is unavailable.
// However, I have already updated the visual style to look like a "track" to imply rail connectivity.
